name: half_ellipse
description: "A customizable half ellipse container for Flutter"
version: 1.0.0
homepage: https://jsontodart.cn/
repository: https://github.com/Matkurban/half_ellipse
topics: [widgets]

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
